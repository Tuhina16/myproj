function {
    const navbar = document.querySelector("navbar-nav ml-auto");
    const icon = document.querySelector("collapse navbar-collapse justify-content-end");
    menu.classList.toggle("open");
    icon.classList.toggle("open");
}